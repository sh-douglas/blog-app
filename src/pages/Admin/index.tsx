import api from "../../api";

export function Admin() {
  async function handleCreatePost(data) {}

  return (
    <div>
      <h1>Admin</h1>
      <button type="button">Criar posts</button>
    </div>
  );
}
